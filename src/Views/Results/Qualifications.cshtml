@{
    ViewBag.Title = $"About Qualifications";
}

@Html.Partial("Back", new {
    href = Url.Action("Index", "Results", (ViewData["Filter"] as ResultsFilterViewModel)?.ToRouteValues()),
    title = "Return to course search",
    aria-label = "Return to course search"
    })

<div class="grid-row">
    <div class="column-full">
        <h1 class="heading-xlarge">
            About Teaching Qualifications
        </h1>
    </div>
</div>